After a successful build, in project settings

1) make sure the excel path is right
2) update the working directory
see image config.jpg.

You can monitor the logs of the app directly in powershell (version >= 3.0) by runing MonitorLogs.ps1